#include <stdlib.h>
#include <stdio.h>

void add(int *v, int inc)
{
	*v = *v + inc;
}

int main()
{
	int value  = 13;
	add(&value, 2);
	printf("Après l'appal : %d\n", value);
	
	return EXIT_SUCCESS;
}
